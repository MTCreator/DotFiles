# Hotkeys

super + Return
	$TERMINAL -e tmux

super + shift + Return
	$TERMINAL

super + d
	dmenu_run

super + q
	$BROWSER

super + r
	thunar

super + e
	$TERMINAL -e "tmux -c 'emacs -nw'"

super + c
	code

super + t
	$TERMINAL -e tasksh

